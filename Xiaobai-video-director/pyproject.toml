[project]
name = "video-director-runtime"
version = "1.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx[socks]==0.28.1",
    "pillow==11.3.0",
    "python-dotenv==1.2.3",
]

[tool.uv]
package = false
